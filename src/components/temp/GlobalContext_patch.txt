PATCHES PARA GlobalContext.jsx

1) Adicione este import no topo:
import { logCategoryView, logTagView } from "../services/stats";

2) Dentro de handleFilter, depois de:
setFilteredResources(filtered);
setFilters([category]);
setSelectedTag(null);

adicione:
logCategoryView(category);

3) Dentro de handleTagFilter, depois de:
setFilteredResources(filtered);
setFilters([]);
setSelectedTag(tag);

adicione:
logTagView(tag);

Nada mais no arquivo precisa mudar para o tracking básico.
